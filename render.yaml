services:
  - type: web
    name: mbse-mbple-workbench
    runtime: static
    branch: 02_Rework_fix_v01
    buildCommand: npm ci && npm run build
    staticPublishPath: ./dist
    autoDeployTrigger: commit
    envVars:
      - key: NODE_VERSION
        value: "24"
