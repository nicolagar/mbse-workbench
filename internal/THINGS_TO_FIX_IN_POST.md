# Things to fix in post

Minor open points to address after all three prompts are incorporated and the complete page workflow has been functionally tested.

## Completed

1. Versions 1.5.2–1.5.3 make the left navigation provide expandable Problem Space and Solution Space workflow sections with direct, domain-separated activity destinations.
2. Versions 1.5.2–1.5.3 make the coffee-machine starter separate product architecture from manufacturing-process/industrial-equipment semantics and allocate resources to the realizing industrial-system components while retaining the full MBSE/MBPLE acceptance chain.
3. Versions 1.5.0 and 1.5.1 established semantic graph bands, crossing-minimized orthogonal routing, relationship filters, progressive disclosure, measured nodes, and directional ports.
4. The established Vite build uses measured manual chunks for graph layout, React Flow, React, PDF, XLSX, charts, icons, and state; the 1.5.2 production build completes without a bundle-size advisory.
5. Version 1.5.2 removes the desktop-only minimum width and adds responsive application-frame and dense-workspace breakpoints for desktop, laptop/ChromeOS-representative, tablet, and phone widths.

## Remaining external verification

Run the live browser/device matrix from GitHub Codespaces or a local checkout. The managed release-candidate environment blocked both local Vite port binding and cloud-Chrome access to a local artifact; the limitation and manual matrix are recorded in `ACCEPTANCE_RESULTS_POST_FIX_V01.md`.

## Keep out of the current Stage-B REV04 correction

These items were intentionally deferred during the Stage-B REV04 correction and subsequently completed without changing schema 9 or rewriting the established graph architecture.
